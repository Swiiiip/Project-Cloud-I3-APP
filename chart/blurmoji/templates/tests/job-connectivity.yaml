apiVersion: batch/v1
kind: Job
metadata:
  name: blurmoji-smoke-connectivity
  namespace: blurmoji
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: smoke-connectivity
          image: blurmoji/tests:latest
          command: ["python", "-m", "src.tests.smoke.run_suite", "--scenario", "connectivity"]
          envFrom:
            - configMapRef:
                name: blurmoji-smoke-config

