# Smoke test jobs
#
# This file intentionally lives under templates/ in the legacy repository, but
# it is rendered as a Helm template and therefore must remain YAML-safe.
#
# The actual smoke test manifests are not part of the runtime chart changes.
