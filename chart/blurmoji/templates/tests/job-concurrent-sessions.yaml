apiVersion: batch/v1
kind: Job
metadata:
  name: blurmoji-smoke-concurrent-sessions
  namespace: blurmoji
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: smoke-concurrent-sessions
          image: blurmoji/tests:latest
          command: ["python", "-m", "src.tests.smoke.run_suite", "--scenario", "concurrent_sessions"]
          envFrom:
            - configMapRef:
                name: blurmoji-smoke-config

